// Timing constants (in milliseconds)
export const TIMING = {
  INITIAL_LOAD_DELAY: 800,
  SEARCH_DEBOUNCE: 300,
  REFRESH_DELAY: 1000,
} as const;

// Avatar sizes (Tailwind units: 1 unit = 4px)
export const AVATAR_SIZE = {
  SMALL: 8,  // 32px
  MEDIUM: 16, // 64px
  LARGE: 24,  // 96px
} as const;

// Scroll amounts
export const SCROLL_AMOUNT = 300; // pixels
